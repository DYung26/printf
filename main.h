#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <limits.h>
#include <unistd.h>

int printf_str1(va_list val);
int _putchar(char c);
int _printf(const char *format, ...);

#endif
